# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Frontend build output (Railway builds this fresh each deploy)
frontend/dist/

# Database files — never commit, Railway uses a mounted volume
*.db
*.db-shm
*.db-wal

# Environment variables — never commit secrets
.env
backend/.env

# Uploads (OCR receipts, etc.)
backend/uploads/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log
